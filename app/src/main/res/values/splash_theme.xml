<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen theme: shown briefly via androidx.core.splashscreen
         while LoginActivity decides where to navigate next (onboarding,
         login, profile setup, or home). Must be applied as the theme of
         the launcher activity (LoginActivity) BEFORE setContentView(). -->
    <style name="Theme.AetherCloud.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/md_surface_light</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/md_primary_container_light</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.AetherCloud.Login</item>
    </style>
</resources>
