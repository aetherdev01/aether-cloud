<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Splash screen theme (dark mode) -->
    <style name="Theme.AetherCloud.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/md_surface</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_splash_logo</item>
        <item name="windowSplashScreenIconBackgroundColor">@color/md_primary_container</item>
        <item name="windowSplashScreenAnimationDuration">500</item>
        <item name="postSplashScreenTheme">@style/Theme.AetherCloud.Login</item>
    </style>
</resources>
